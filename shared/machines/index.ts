// Behavior SSOT: central export for statecharts
// For now, re-export the existing client chat machine factory so consumers can import from shared.
export { createChatMachine } from '../client/src/state/chatMachine';


